name: Link Issues

on:
  push:
    branches:
      - main
      - develop

jobs:
  link-issues:
    runs-on: ubuntu-latest
    steps:
      - name: Link commits to issues
        uses: peter-evans/link-issue@v1
        with:
          repo-token: ${{ secrets.PERSONAL_ACCESS_TOKEN }}



